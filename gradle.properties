org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Recommended by AGP 9 for faster dependency-model import. The project has
# no library modules that require dependency constraints between configurations.
android.dependency.useConstraints=false
